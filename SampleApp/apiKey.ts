// The only place this sample keeps the API key. Replace the placeholder with the
// pk_live_… key of your app from console.appspike.dev; until then the sample refuses
// to initialize and says so on screen. (Typed `string` rather than the inferred literal
// so that replacing the placeholder leaves the placeholder check well-typed.)
export const API_KEY: string = 'YOUR_API_KEY';

export const API_KEY_IS_PLACEHOLDER = API_KEY === 'YOUR_API_KEY';
